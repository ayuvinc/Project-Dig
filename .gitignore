# Claude Code local settings (personal overrides, not committed)
.claude/settings.local.json

# Environment — never commit secrets
.env
.env.*
.env.local
!.env.example

# Next.js
.next/
out/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Framework runtime files — transient, not project state
tasks/.session-transition-lock
